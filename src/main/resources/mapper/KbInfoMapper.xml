<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.aiassistant.mapper.KbInfoMapper">
    <select id="countByCanonicalField" resultType="int">
        SELECT COUNT(*) FROM kb_info WHERE canonical_field=#{canonicalField}
    </select>

    <insert id="insert" parameterType="com.aiassistant.domain.KbInfo" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO kb_info(canonical_field, description, aliases, create_time, update_time)
        VALUES(#{canonicalField}, #{description}, #{aliases}, #{createTime}, #{updateTime})
    </insert>
</mapper>
